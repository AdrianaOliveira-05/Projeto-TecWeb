O que falta:
- tentar meter as setas diagonais direitas (quando tem duas numa so caixa)
- se desistirmos do jogo tem de mostrar a classificaçao que ficou ate aí e mostrar o resultado na tabela das classificaçoes
- existem modo facil, medio e dificil em IA, temos de alterar isso
- meter as jogadas da IA a acontecer mais devagar, para se perceber melhor o que esta a acontecer
- aparecer mensagens quando as jogadas nao sao possiveis
- só começa o jogo quando dá 1 o dado
- uma peça nao pode avançar outra, ainda que da mesma cor


// Restrição: primeira jogada só pode ocorrer com dado = 1
if (jogadorAtual === "A") {
  const algumaMovida = tabuleiroDados.flat().some(p => p?.owner === "A" && p.moved);
  // Se nenhuma peça azul se moveu ainda
  if (!algumaMovida) {
    if (valorDadoAtual !== 1) {
      mensagemTexto.innerText = "⚠️ O dado não deu 1. Não podes começar. Passa a vez ao computador.";
      // consome o dado e passa turno
      valorDadoAtual = null;
      resultadoDado.textContent = "Clique para lançar";
      setTimeout(() => alternarJogador(), 1000);
      return;
    } else {
      // ✅ dado = 1 → pode começar o jogo normalmente
      mensagemTexto.innerText = "🎯 Dado = 1! Podes começar o jogo.";
    }
  }
}
